#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
  FILE *fptr = NULL;
  fptr = fopen("lsit.text", "w");
  if (fptr == NULL) {
    printf("%s\n", "error!");
    exit(1);
  }
  fprintf(fptr, "hello file!\n");
  fclose(fptr);

  return 0;
}
